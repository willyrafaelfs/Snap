🚀 Langkah Implementasi yang Disarankan
Fase 1: Core Booth (MVP)
Inisialisasi kamera getUserMedia + error handling permission.

Video playback + Canvas capture (single shot).

Tombol capture + flash overlay + shutter sound.

Galeri sederhana (download satu foto).

Fase 2: Kreasi & Ekspresi
Implementasi layer stiker (drag/resize dengan pointer events).

Pustaka filter CSS (filter: sepia(1) contrast(1.2)).

Layout frame (Overlay canvas di atas hasil foto).

Timer countdown animasi + logika burst.

Fase 3: Sharing & Polish
Bottom sheet galeri dengan animasi.

Integrasi Web Share API untuk mobile.

QR code generation (pakai library qrcodejs) untuk link download koleksi foto.

Tambahkan confetti & efek perayaan.

Responsive mode (CSS media query untuk orientasi).